import styled from 'styled-components';

export const GenderSelectorContainer = styled.div`
	width: 100%;
	margin-right: ${(props) => (props.hasSpacing === true ? '1rem' : '0')};
`;

export const GenderSelectorsContainer = styled.div``;
